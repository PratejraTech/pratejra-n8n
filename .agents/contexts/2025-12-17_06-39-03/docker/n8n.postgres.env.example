# Purpose: Example environment file for n8n + Postgres single-host Docker Compose topology (DO NOT put secrets in git)
# Created/Updated: 2025-12-17 00:00
# Agent: GPT-5.2

# ---- n8n runtime ----
N8N_HOST=localhost
N8N_PROTOCOL=http
WEBHOOK_URL=http://localhost:5678/

# Required: keep stable across restarts/restores or encrypted credentials will break.
N8N_ENCRYPTION_KEY=change-me-to-a-long-random-string

# Optional: basic auth for UI (recommended if exposed)
N8N_BASIC_AUTH_ACTIVE=false
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=change-me

# Metrics / diagnostics
N8N_METRICS=true
N8N_DIAGNOSTICS_ENABLED=false

# ---- Postgres (used by postgres service + injected into n8n via compose) ----
POSTGRES_DB=n8n
POSTGRES_USER=n8n
POSTGRES_PASSWORD=change-me


